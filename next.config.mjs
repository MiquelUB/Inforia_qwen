/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // No necesitamos experimental: { appDir: true } en Next.js 14+
};

export default nextConfig;
